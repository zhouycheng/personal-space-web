# 我的画布

画布使用 ReactFlow 展示仓库维护的卡片和连线。内容唯一来源是 `src/components/mine-canvas/mineCanvasData.ts`；Agent 通过项目技能 `justinweb-canvas-content` 修改内容、默认布局、样式和卡片组件，构建发布后生效。

## 浏览与位置

- 网页不提供内容编辑、作者登录、上传、尺寸或连线编辑。
- 直接拖动卡片调整位置，无额外把手。浏览器仅在 `justin-canvas-positions-v1` 保存被移动卡片的 ID 与 x/y。
- 本地位置覆盖同 ID 的默认位置；新增卡片采用默认位置，已删除 ID 忽略。内容始终来自网站。
- 恢复默认布局清除本地覆盖。清除站点数据、首次访问或存储不可用时采用默认布局。
- 缩放与视角不持久化；进入及可用画布尺寸变化时自动适配内容。
- 双击卡片聚焦，双击空白区域查看全图；键盘 Enter/空格聚焦节点。内容直接在节点展示，没有阅读面板。
- 手机内容列表收进抽屉，点击条目定位卡片。空白区域平移，双指缩放。

## 内容与资源

数据类型见 `mineCanvasTypes.ts`，共用只读渲染器为 `CanvasCardContent.tsx`。HTML 字段只接受经审核的仓库内容。图片放在 `public/canvas/`。自定义卡片通过扩展类型和 React 渲染器实现。

画布没有数据库、会话、修订保存 API 或备份服务。健康接口检查静态画布结构及桌面内容。

2026-09-24 首次迁移仅核对了本地 revision 0（7 张示例卡片、6 条连线），未包含线上实际内容。现已以 zhoust.cn revision 138 为基准修正：4 张真实卡片（名片、竞赛经历、动态状态、斯宾诺莎引用）、2 条连线，保留线上稳定 ID、原文及默认位置。线上内嵌头像迁移至 `/canvas/justin-avatar.jpg`。核对 checksum：`3df4aed57993a8432fa13120c304906d05c916736f863fb5948699e07e22a352`。

动态状态继续读取同源 `/api/activity/current`，区分加载、在线、离线和请求失败；到期的快照不再显示为在线。卡片恢复线上类型样式，桌面和手机均直接缩放浏览节点内容。
