# 当前工作

## 当前焦点

三维工作室首页连接 Justin OS、文件夹与 ReactFlow 画布，提供物件交互、本地时间光照和可访问的内容入口。

## 活跃界面

- `src/pages/index.astro`：`/` 首页别名，渲染共享应用外壳。
- `src/pages/home.astro`、`src/pages/works.astro`、`src/pages/canvas.astro`、`src/pages/os.astro`：工作室、文件夹、画布和 OS 路由入口。
- `src/components/app/JustinAppShell.astro`：共享路由外壳与工作室、OS、文件夹、个人画布挂载点。
- `src/components/app/studioAppRuntime.ts`：URL 驱动的 History API 导航、空间动画；返回复用已有首页记录。
- `src/components/studio/`：程序化 Three.js 房间、三个内容入口、转椅动画、键盘/失败替代入口及本地时间光照插值。
- `src/components/app/homeRuntimeState.mjs`：保留的旧终端辅助，不再用于首页。
- `src/styles/global.css`：全局布局、启动屏幕动效、Dock 导航、个人画布、响应式规则和 OS 投影样式。
- `src/justin-kit/components/macos-desktop/`：可复用的 macOS 风格桌面和窗口系统。
- `src/justin-kit/components/symbol-dome-background/`：Justin OS 桌面背景的单面符号半球组件。
- `public/os-desktop/`：文件驱动的 Justin OS 桌面内容。
- `src/justin-kit/components/local-activity-status/`：本地活动运行时和监控。
- `src/components/mine-canvas/`：ReactFlow 画布编辑器、七种卡片类型和内联编辑；首次进入 `/canvas` 时才加载。
- `src/server/canvas/`：不可变 SQLite revision、作者会话和内容寻址图片资源。
- `ops/backup/`：每小时 SQLite 一致性快照、本机 Restic 与 S3 异地备份。

## 验证基线

- `rtk npm run build`。
- `rtk node --test tests/*.test.mjs`（App 外壳辅助逻辑）。
- 交互密集的变更需要浏览器预览，包括直接刷新 `/`、`/home`、`/works`、`/canvas` 和 `/os`。
- 本地活动变更需要环境和路由检查。

## 工作室功能与验证

- 文件卡片的封面、标题和摘要整组旋转：左侧向左倾斜 14°，当前项摆正，右侧向右倾斜 14°；两侧缩至 88%，整组使用 65% 不透明度，封面叠加 18% 暗色遮罩降低注意力。当前项完全清晰，切换时平滑更新，悬停不改变姿态与聚焦关系。macOS Safari 已复现侧卡滤镜导致的紫色横线，移除侧卡滤镜后复查消失；工作室与详情的背景高斯模糊保留。
- 2026-09-14 吸附调整：按用户确认改为单卡居中、25% 位移阈值与每次最多切换相邻一张，统一触摸/鼠标拖动和滚轮行为。49 项 Node 测试及构建通过；Chromium 桌面和触摸模拟验证阈值两侧、弹回中间帧、正反向、首尾边界、取消手势、滚轮、详情关闭、键盘、减少动态效果与横竖屏切换。类型检查仍为原符号半球组件的 19 处空值错误，未验证真实手机手感。
- 2026-09-14 文件夹：直立文件盒、卡片和计数共用清单，新增附件简历的网页阅读版；修复手机场景 `pointerup` 切换页面后，后续 `click` 命中新出现首项的误打开。移除鼠标位置驱动的旧横移算法，增加有效点击校验和文件清单测试。Node 22.22.3 下 48 项测试与构建通过，类型检查仍为符号半球组件的 19 处既有空值错误。Chromium 在 1280×800、360×732、390×844、844×390 检查了模型近景/侧面、触摸进入及惯性滑动、鼠标拖动/滚轮、键盘激活与原生模态、简历长文/固定关闭按钮、位置与焦点恢复、减少动态效果、重复关闭、动画中途返回及旋转屏幕；各路由直达和刷新、文件夹往返保留视角、WebGL 上下文丢失后的 HTML 文件入口通过。背景模糊保留；新增模型资源复用原有几何体、材质与纹理释放登记。真实手机 Safari/微信及长期性能未验证，未部署。
- Home 探索面板取代整组按钮展开：窄屏及触摸设备只常驻一个入口，桌面通过键盘访问同一面板。「去逛逛」提供四个内容入口，「视角」提供倍率、重置和按需展开的方向按钮，「摆件」提供灯、钟、三层抽屉与转椅操作。加载中或 WebGL 失败时保留内容入口；失败时可重试。面板开关不改变历史，路由变化时关闭，灯/钟/抽屉状态与模型直接点击同步。
- Home 场景首帧绘制成功后才启用场景控制。模块加载、模型初始化、上下文创建、着色器、绘制和上下文丢失分别保留错误；`/home?studioDebug=1` 在探索的「去逛逛」页提供可选中或复制的诊断详情（协议、浏览器、上下文属性、失败阶段和堆栈），仅保留当前页面内存中最近 12 条，无日志后端。失败前未取得上下文时 WebGL 能力显示未知，不能据此判定设备不支持。
- 上下文创建、着色器或绘制失败可自动轻量重试一次，关闭抗锯齿、限制像素比为 1 并关闭实时阴影，沿用相同模型与照明交互；普通脚本异常不自动降画质。上下文丢失暂停绘制并释放失效 GPU 句柄，恢复后重建资源并重新绘制；手动重建保留视角、台灯、时钟显示模式与抽屉状态，刷新恢复默认。场景故障中断过渡时按当前 URL 落到稳定页面。
- 2026-09-13 iPhone 兼容修复：47 项 Node 测试及构建通过，类型检查仍为符号半球文件的 19 处既有空值错误。Chromium 浏览器故障注入验证上下文、着色器、绘制的一次轻量重试，连续失败停止，模块/初始化异常分类与清理；上下文丢失/恢复及销毁后不恢复、手动重建状态保留通过。验证了桌面拖动/缩放、390px 窄屏布局、OS/画布/作品集返回、过渡中丢失上下文、各路由直达、减少动态效果与模拟后台暂停恢复。Playwright WebKit 浏览器未安装；用户设备微信和 Safari 均失败的具体异常仍待真机诊断，不能以本地浏览器模拟宣称真机修复成功；本次未部署。
- 面板使用原生 dialog、语义标签和 details；支持关闭按钮、遮罩、Esc、Tab 焦点循环、标签方向键以及倍率/角度边界禁用后的焦点保留。打开时冻结背景拖动，摆件动画继续；正常展开 220ms、关闭 160ms，减少动态效果时直接显隐。触摸控件至少 44px，横屏和展开后的内容在面板内部滚动。
- 2026-09-12 探索面板验证：46 项 Node 测试及构建通过，类型检查仍为符号半球文件的 19 处既有空值错误。浏览器检查了 390×844、360×800、844×390 和 1280×720 的入口与面板，覆盖倍率/方向边界、反向与重置、灯/钟/抽屉状态、电子钟直接点击同步、Tab 循环和 Esc/遮罩关闭、拖动中开面板冻结相机、展开中间帧、OS/画布/作品集往返及历史前进关闭面板；各路由直达与 Home 刷新正常。暂停场景脚本验证加载入口，主动丢失 WebGL 上下文验证失败入口，模拟 pagehide/pageshow 验证面板关闭及渲染暂停恢复；测试拦截与模拟设置均恢复，最终页面无控制台错误。真实手机触摸、系统文字放大与 VoiceOver 尚未验证。
- Home 滚轮缩放范围为 `0.85×–2.2×`，默认 `1×`；缩放、旋转和重置视角共用按帧时间计算的连续阻尼，输入只更新目标，不重启动画。支持同时滚动与拖动、滚轮单位换算，保留 Ctrl/Command + 滚轮的浏览器行为。HTML 键盘入口提供放大、缩小和重置视角。OS、画布和作品集往返保留倍率和旋转，刷新恢复默认；页面过渡期间禁用场景缩放。
- MacBook 增加大小键、方向键、键帽字符纹理、扬声器孔阵、触控板边缘、接口和转轴层次；咖啡杯补充圆滑杯垫、杯口、把手连接、液面和泡沫，热气用共享纹理的少量粒子表现。热气仅在可交互的 home 持续绘制，后台或其他页面暂停，减少动态效果时隐藏；热气不参与拾取或阴影，静态阴影复用。
- 2026-09-12 缩放验证：构建与 43 项 Node 测试通过，覆盖滚动单位、倍率上下限、大输入和边界反向；1280×720 与 390×844 浏览器检查了构图、多角度近景、缩放中间帧、悬停与拖动衔接、物件点击、键盘缩放/重置、减少动态效果、各路由直达、OS/画布/作品集往返及画布前进后退。模拟 visibilitychange 验证了后台暂停和恢复；独立场景销毁检查确认 canvas 移除及几何体、材质、纹理释放，浏览器无控制台错误。类型检查仍为符号半球文件的 19 处已有空值错误；真实设备滚轮手感和长期性能仍待设备端验收。
- 居中独立桌椅与底部署名布局；背景、日光与桌面台灯按访客本地时钟连续插值，运行时每秒同步一次。
- 台灯采用带厚度和封口的灯罩、双杆支臂与关节，点击可开关灯罩发光和桌面照明；手动关闭后不会被时间同步重新点亮。电子钟位于台灯左侧，默认显示 `HH:MM`，点击切换 `MM.DD`，再次点击恢复时间。
- 三层抽屉独立点击开合，使用中空柜体、抽屉盒和滑轨；第一层的日记本进入站内 `/journal` 实体书阅读，正文由 Markdown 在构建时自动分页。键盘入口同步抽屉展开、台灯开关和日期模式状态；减少动态效果时抽屉直接到达终态。日记职责与写作说明见 `../features/journal.md`。
- 桌面直立文件盒采用奶油白盒体、低前挡板和斜切侧板；内部文件由 `studioFiles.ts` 清单生成，项目为有封面、纸页和名称书脊的文件夹，简历为带姓名、岗位和章节排版的双面薄纸。按实际厚度向左紧密排列，文件以下边缘为支点倾靠，简历贴靠封面并轻微外弯；直接保存放稳后的静态姿态，不运行启动物理动画，也不为文件继续请求帧。倾角和展开范围随文件数量收缩。咖啡杯包含杯壁、杯口、把手、杯垫和咖啡液面。
- 2026-09-12 桌面物件验证：构建与 42 项 Node 测试通过；内置浏览器检查了 1280×720、390×844、多角度灯罩、夜间开关光照、抽屉中间帧与连续反向点击、键盘入口、减少动态效果、日记本实际外链跳转，以及作品集/OS/画布往返与画布过渡中刷新。浏览器无控制台错误；类型检查仍为下述符号半球文件的 19 处已有空值错误。
- 桌面平放的 iPad 打开“我的画布”，直立文件盒打开横向文件卡片；电脑和 iPad 始终固定在场景中，共用约 1.8 秒的镜头过渡：先转向屏幕正面，再沿屏幕法线靠近，实时页面在靠近过程中贴屏渐显并接管全屏，返回时逆向收回。个人画布保留原编辑器、鉴权和数据接口。
- 水平拖动限制在正面左右各约 70°，相机的俯仰参数限制为 `0.2–1.0`，最近倍率下仍保持在桌面前沿之前；拖到边界后反向操作立即更新目标。悬停与触摸按下仅轻微高亮当前部件、显示提示，不自动移动相机。相机移动复用静态阴影，只有实体动画和灯光更新触发阴影刷新。场景延伸至底部，署名直接叠放，桌面字号 16px、窄屏 13px。
- 2026-09-12 相机修正：45 项 Node 测试通过，新增正面角度边界与连续阻尼检查；浏览器检查了 1280×720 和 390×844、左右极限、边界反向、缩放与拖动同时输入、减少动态效果及 OS 往返。咖啡、杯垫和热气整体向左移开，在近景和侧视角下与电脑留出明确间距。
- 默认镜头位于桌椅左后方，主体居中，返回保留离开时的倍率和角度；iPad 平放于桌面右侧，点击后镜头转向屏幕正面并靠近。
- 文件卡片支持触摸、鼠标拖动、滚轮和方向键浏览，以一张卡片居中停靠。松手位移不足中心间距的 25% 则平滑弹回，达到阈值则切换相邻一张；滚轮以 140ms 无输入作为一次手势结束，采用相同阈值。取消手势回到当前文件，旋转屏幕保持当前居中项，减少动态效果时直接归位。点击侧卡先居中，点击当前卡片才打开详情；进入页面的触摸和拖动不触发文件打开。键盘聚焦卡片时先居中，Enter/Space 打开。底部显示真实文件数量，关闭详情恢复卡片焦点和位置。当前包含 FrameLean、QandA 与附件简历的网页阅读版，工作室及详情遮罩保留高斯模糊。
- 16 英寸 MacBook Pro（M2 Pro，2023）深空灰低多边形模型，依据 [Apple 规格](https://support.apple.com/en-gb/111838) 的 35.57 × 24.81 cm 比例和官方外观图制作，桌面占宽约 27%。
- 转椅用 2 秒完成整椅旋转，万向轮对齐运动切向并按路径长度滚动；采用受控运动曲线表达惯性感。
- 42 项 Node 测试覆盖现有功能、工作室稳定态恢复、时间光照、转椅运动曲线、摄像机贴近距离及页面淡入时机与作品集缓动边界。
- 路由回归：Chrome 实测“作品集返回首页 → 进入 OS → 浏览器后退”回到 `/home`；`/os` 直达和刷新、`/canvas` 动画中刷新、画布前进后退均与 URL 一致。42 项测试与构建通过。
- 本轮在 Chrome 检查了书架/画板实际点击、OS 与画布放大和收回中间帧、画布刷新恢复、作品详情、390×844 作品集布局、减少动态效果、前进后退；横移 45 帧采样无大幅位置跳变。构建与差异检查通过。Safari 的实际手感仍需设备端复核。
- 本机 macOS 内置浏览器检查了 1280×720 与 390×844 布局、物件点击、面板 Escape、键盘入口、OS 往返、Markdown 窗口、路由刷新与前进/后退、过渡中切路由、WebGL 降级、减少动态效果和时间光照。预览数据位于独立 `.workspace/alpha-data/`。

## 当前风险

- 构建提示部分 chunk 超过 500 kB，Three.js 已动态导入。
- `npx tsc --noEmit` 当前在 `src/justin-kit/components/symbol-dome-background/symbol-dome-background.ts` 报告 Canvas 和绘图上下文的 strict-null 检查错误。
- 真实手机 30fps 目标、长时间 GPU/内存稳定性及生产 Docker 部署未验收。
- 首页和 Justin OS 过渡交互密集，需要在桌面和窄屏幕上进行视觉 QA。
- 路由外壳变更需要后退/前进、刷新与动画中途导航检查。旧版本已写入的浏览器历史无法批量清除，回归测试应从新首页记录开始。
- `public/os-desktop/` 内容在构建时扫描，重命名文件需要页面刷新或重新构建。
- 画布实时编辑通过单通道保存队列写入不可变 revision；需要持续验证离线重试和 `409` 冲突状态。
- Docker 生产运行时必须从 `dist/client/os-desktop` 扫描桌面内容；健康检查会阻止空桌面部署被视为正常。
