# Playground

`src/playground/` 保存已经完成讨论、但未直接进入正式体验的草图和设计探索。

这里的文件不是运行时入口，也不应被页面直接导入。被采纳的方案应吸收到 `src/justin-kit/components/` 或正式业务组件中，并在对应组件 README 里记录来源。

当前归档：

- `symbol-dome-background/`：Justin OS 背景动效探索，最终采纳为 `src/justin-kit/components/symbol-dome-background/`。
