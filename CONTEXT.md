# JustinWeb 上下文

本文档记录项目词汇，应在规划、实现、测试和设计讨论中保持一致。

## 首页状态词汇

讨论首页工作室体验时使用以下名称：

- `room`：默认工作室全景，当前为独立桌椅和 iPad 布局，可以拖动视角、点击物件或使用 HTML 入口。
- `entering`：点击电脑后，镜头先转向显示器正面，再沿屏幕法线靠近；Justin OS 在屏幕填满视口的过程中渐显接管。
- `desktop`：全屏 Justin OS 桌面，房间暂停绘制。
- `returning`：从 Justin OS 返回工作室，镜头沿显示器法线后退并回到默认房间视角。
- `entering-canvas`：点击 iPad 后，镜头先转向平板正面，再沿屏幕法线靠近；个人画布在平板填满视口的过程中渐显接管。
- `canvas`：全屏个人画布，复用既有 ReactFlow 编辑器，房间暂停绘制。
- `returning-canvas`：从个人画布返回工作室，画布收回的同时镜头沿 iPad 法线后退至全景。

URL 是唯一界面状态来源：`/home` 对应房间，`/os` 对应桌面，`/canvas` 对应个人画布，`/works` 对应作品集。导航开始即更新地址，动画只负责视觉过渡；中途刷新按 URL 打开稳定界面。返回工作室复用已知首页历史记录，无法确定来源时替换当前记录，不猜测上一页。

这些术语同时描述空间动画状态和稳定路由状态，便于实现、测试与文档保持一致。

## 文档入口

- `README.md`：当前运行时、命令、实现状态和项目结构。
- `CHANGELOG.md`：版本级变化和已验证的里程碑。
- `docs/README.md`：仓库文档索引。
- `docs/work/`：当前工作、待办和决策。
- `docs/develop/workflow.md`：持久化的工作流和验证规则。
- `.agents/skills/README.md`：项目级技能矩阵和路由。
